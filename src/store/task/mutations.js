export const ADD_TASK = (state, task) => {
  state.task.push(task)
}

export const SET_TASK = (state, task) => {
  state.task.push(task)
}

export const CLEAR_TASK = (state, task) => {
  state.task = task
}
